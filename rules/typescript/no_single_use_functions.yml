id: typescript-no-single-use-functions
language: TypeScript
severity: warning
message: This immediately invoked function is used once; inline its body unless the scope is essential.
rule:
  pattern: ($FUNCTION)($$$ARGS)
constraints:
  FUNCTION:
    any:
      - kind: function_expression
      - kind: arrow_function
